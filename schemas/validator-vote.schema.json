{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "validator-vote.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "candidateFingerprint", "verdict", "reachable", "level", "evidence", "reason"],
  "properties": {
    "version": { "type": "string", "const": "v2" },
    "candidateFingerprint": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "verdict": { "type": "string", "enum": ["confirm", "reject", "split"] },
    "reachable": { "type": "boolean" },
    "level": { "type": "string", "enum": ["blocker", "major", "minor", "suggestion"] },
    "evidence": { "type": "string", "minLength": 1, "maxLength": 4000 },
    "reason": { "type": "string", "minLength": 1, "maxLength": 4000 }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "verdict": { "const": "confirm" }
        },
        "required": ["verdict"]
      },
      "then": {
        "properties": {
          "reachable": { "const": true }
        },
        "required": ["reachable"]
      },
      "else": {
        "properties": {
          "reachable": { "const": false },
          "level": { "const": "suggestion" }
        },
        "required": ["reachable", "level"]
      }
    }
  ]
}
